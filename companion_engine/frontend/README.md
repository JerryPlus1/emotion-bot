# Companion Engine Frontend

这是 Companion Engine 的 React + Vite + TypeScript 网页 Demo。

## 启动

先启动后端：

```bash
cd ../backend
uvicorn main:app --reload
```

再启动前端：

```bash
npm install
npm run dev
```

访问：

```text
http://127.0.0.1:5173
```

默认 API 地址：

```text
http://127.0.0.1:8000
```
